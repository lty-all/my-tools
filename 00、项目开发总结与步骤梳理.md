# DevPilot AI Toolkit 项目开发总结与步骤梳理

> 本文档是对 1~9 号设计文档的综合梳理，用于快速回顾全局规划、明确初始开发步骤与后续演进路线。
>
> 项目定位：**面向全球开发者的一站式 AI + Developer Tools 工作台 SaaS 平台**
>
> 目标用户：20-40 岁的前端 / 后端 / 全栈 / DevOps 开发者（美国、欧洲、印度、东南亚、中国）

---

## 一、文档体系总览

| 编号 | 文档 | 作用 | 状态 |
| --- | --- | --- | --- |
| 1 | 产品 PRD | 产品定位、用户场景、模块规划、会员体系、盈利模式、开发路线 | ✅ 完整 |
| 2 | 技术设计文档（TDD） | 前后端分离方案、技术栈、模块划分、AI 架构、部署设计 | ✅ 完整 |
| 3 | 数据库详细设计 + API 接口文档 | Prisma Schema、8 张表、REST API 规范、额度控制 | ✅ 完整 |
| 4 | 前后端代码架构 + Monorepo 初始化方案 | pnpm workspace、目录结构、依赖、Git 规范、Trae 用法 | ✅ 完整 |
| 5 | 第一阶段开发任务拆解（30 天执行计划） | Day1-Day30 每日任务、Trae Prompt、时间建议 | ✅ 完整 |
| 6 | Sprint 1：Monorepo + Vue3 + NestJS 实战步骤 | 工程骨架搭建命令与配置 | ✅ 完整 |
| 7 | Sprint 2：用户系统 + JSON Formatter | Auth 模块、工具系统、历史记录闭环 | ✅ 完整 |
| 8 | Sprint 3：AI 模块设计与多模型接入 | Provider 架构、DeepSeek/GLM/Gemini、额度限制 | ✅ 完整 |
| 9 | Sprint 4：Docker 生产部署 + Nginx + Cloudflare + Jenkins CICD | 生产部署 | ⚠️ 空文件（待编写） |

---

## 二、技术与架构总览

### 2.1 技术栈

```
前端：Vue3 + TypeScript + Vite + Pinia + Vue Router + TailwindCSS + Axios
后端：NestJS + Prisma + PostgreSQL + Redis
AI：  统一 AIProvider 接口（DeepSeek / GLM / Gemini / OpenAI 兼容）
工程：pnpm workspace（暂不用 Turbo）+ Docker Compose
部署：Nginx + Cloudflare + Jenkins CI/CD
开发：Windows + WSL2 Ubuntu + Trae
```

### 2.2 Monorepo 结构

```
devpilot-toolkit
├── apps
│   ├── web          # Vue3 前端
│   └── api          # NestJS 后端
├── packages
│   ├── types        # 前后端共享类型
│   ├── ui           # 公共组件
│   └── config
├── docker
├── docker-compose.yml
├── pnpm-workspace.yaml
└── package.json
```

### 2.3 系统架构

```
用户 → Cloudflare → Nginx → Vue 静态文件
                          → NestJS API → Prisma ORM → PostgreSQL
                                       → Redis
                                       → AI Provider → DeepSeek / GLM / Gemini
```

### 2.4 数据库核心表

| 表名 | 用途 | 阶段 |
| --- | --- | --- |
| User | 用户（GitHub/Google/Email 登录） | MVP |
| Tool | 工具元数据 | MVP |
| ToolCategory | 工具分类 | MVP |
| ToolHistory | 用户使用记录 | MVP |
| AIUsage | AI 调用与 Token 统计、额度控制 | Sprint 3 |
| Subscription | 会员（Stripe） | 后续 |
| ApiKey | 开放 API（预留） | 后续 |
| Article | SEO 博客内容（预留） | 后续 |

### 2.5 API 规范

- 统一前缀：`/api/v1`
- 模块：auth / users / tools / ai / history / subscription / payment
- 认证：JWT（Bearer Token）
- 文档：Swagger

---

## 三、初始开发步骤梳理（MVP 骨架 → 第一个闭环）

> 对应 30 天计划 Week1-Week2、Sprint 1、Sprint 2。目标：项目可运行 + 第一个业务闭环。

### Step 1：工程骨架（Sprint 1 / Day 1-7）

1. **创建 Monorepo**
   - `mkdir devpilot-toolkit && cd devpilot-toolkit`
   - `git init` + `pnpm init`
   - 创建 `pnpm-workspace.yaml`（`packages: [apps/*, packages/*]`）
   - 首次提交：`chore: init project`

2. **初始化 Vue3 前端（apps/web）**
   - `pnpm create vite web`（Vue + TypeScript）
   - 安装：vue-router、pinia、axios、tailwindcss
   - 建立目录：api / assets / components / layouts / pages / router / stores / hooks / utils / types
   - 提交：`feat: setup vue app`

3. **初始化 NestJS 后端（apps/api）**
   - `nest new apps/api`（选 pnpm）
   - 创建模块：auth / users / tools / ai / history + common / config / prisma
   - `main.ts` 配置 `setGlobalPrefix("api/v1")` + Swagger
   - 提交：`feat: setup nest api`

4. **Docker 开发环境**
   - `docker-compose.yml`：postgres:16 + redis:7
   - `docker compose up -d` 启动数据库
   - 提交：`feat: add docker environment`

5. **Prisma 接入**
   - `pnpm add prisma @prisma/client` + `npx prisma init`
   - `.env` 配置 `DATABASE_URL`
   - 创建第一个 User model + `npx prisma migrate dev`
   - 提交：`feat: add prisma database`

6. **前后端联调**
   - Vue 封装 `src/api/request.ts`（axios baseURL）
   - Nest 创建 `GET /api/v1/tools` 测试接口 + 开启 CORS
   - 提交：`feat: setup api communication`

7. **收尾**
   - README、环境变量、Git 规范、Tag `v0.1.0`

### Step 2：用户系统（Sprint 2 / Day 8-10）

1. **数据库迁移**：User / Tool / ToolHistory，`npx prisma migrate dev --name add_user_tool`
2. **Auth 模块**：安装 `@nestjs/jwt passport passport-jwt bcrypt`
   - `POST /api/v1/auth/register`（bcrypt 加密）
   - `POST /api/v1/auth/login`（返回 access_token）
   - `GET /api/v1/auth/me`（JWT 守卫）
3. **前端登录页**：Login.vue / Register.vue，token 存 localStorage
4. **Axios 拦截器**：自动携带 `Authorization: Bearer <token>`
5. **路由守卫**：`router.beforeEach` 校验 `meta.auth`
6. 提交：`feat: add user authentication`

### Step 3：工具系统 + 第一个工具（Sprint 2 / Day 11-14）

1. **工具列表**：`GET /api/v1/tools` 返回卡片数据，Tools.vue 展示
2. **JSON Formatter**
   - 页面：`/tools/json-formatter`
   - 前端：输入 / 格式化 / 压缩 / 错误提示 / 复制
   - 后端：`POST /api/v1/tools/json/execute`
   - 历史记录：登录后写入 ToolHistory
3. 提交：`feat: add tool system` + `feat: add json formatter`

### Step 4：补齐 10 个免费工具（Week2 / Day 11-14）

```
JSON Formatter · JWT Decoder · Base64 · UUID · Regex Tester
Markdown Preview · Timestamp · SQL Formatter · Diff Checker · QR Generator
```

> 完成后：项目从「工程骨架」升级为「可用 SaaS MVP」，具备用户闭环 + 数据闭环 + 第一个 SEO 页面。

---

## 四、后续开发步骤梳理（AI 能力 → 商业化 → 上线运营）

### Step 5：AI 模块（Sprint 3 / Week3 / Day 15-21）

1. **AI Provider 架构**（核心，避免硬编码模型）
   ```
   Controller → AIService → AIProvider(interface)
                              ├── DeepSeekProvider（OpenAI 兼容 SDK）
                              ├── GLMProvider（OpenAI 兼容）
                              └── GeminiProvider（独立封装）
   ```
   - 安装 `openai` SDK，统一 `chat(prompt): Promise<string>` 接口

2. **AI 产品功能**
   - `POST /api/v1/ai/explain`：AI Code Explain
   - `POST /api/v1/ai/debug`：AI Error Fix
   - `POST /api/v1/ai/commit`：AI Commit Generator（Conventional Commit）
   - 前端：pages/ai/{CodeExplain,ErrorFix,CommitGenerator}.vue

3. **额度系统**
   - 新增 AIUsage 表（userId / model / tokens / feature / createdAt）
   - 免费用户每天 5 次、每次最多 4000 Token
   - `AiLimitGuard` + JWT 双守卫
   - Token 统计：保存 `result.usage.total_tokens`

4. **安全设计**
   - 禁止前端直连模型（Key 仅在后端）
   - DTO `@MaxLength(20000)` 输入限制
   - 固定系统 Prompt 防 Prompt 注入

5. 提交规划：
   - `feat: add ai provider architecture`
   - `feat: integrate deepseek model`
   - `feat: add code explain feature`
   - `feat: add ai usage limit`

### Step 6：生产部署（Sprint 4 / Week4 / Day 22-25，文档待编写）

1. **生产 Docker**：web / api 各自 Dockerfile
2. **服务器部署**（2 核 2G）：Nginx + Vue + Nest + Postgres + Redis
3. **域名 / CDN**：Cloudflare A 记录 + SSL + CDN
4. **CI/CD**：git push → GitHub → Jenkins → docker build → docker compose up → restart
5. **注意**：AI 模型走云端 API，不在本地部署

### Step 7：SEO 与增长（Week4 / Day 26-27）

1. 每个工具独立 SEO 页面（`/tools/json-formatter`）
2. Title / Description 规范化（如 "Free JSON Formatter Online"）
3. 博客内容（Article 表预留）：How to format JSON / debug JWT 等
4. 接入 Google Analytics：访问量、热门工具、用户来源

### Step 8：体验优化与发布（Week4 / Day 28-30）

1. 首页 / 移动端 / Loading / 错误页优化
2. 全链路测试：注册 → 登录 → 工具 → AI → 部署
3. 正式发布渠道：Product Hunt / Hacker News / Reddit 开发者社区

### Step 9：商业化与生态（30 天后持续迭代）

| 方向 | 内容 |
| --- | --- |
| 会员订阅 | Stripe 接入，FREE / PRO($9) / TEAM($29) |
| 开放 API | ApiKey 表，按次计费（10000 次 $5） |
| 广告 | Google Adsense（SEO 流量起来后） |
| Affiliate | 推荐 Copilot / Cursor / Vercel / Cloudflare |
| 多模型扩展 | Claude / OpenAI / Qwen / MiniMax（新增 Provider 即可） |

---

## 五、关键里程碑检查表

### Sprint 1（工程骨架）

- [ ] pnpm workspace + Vue3 + NestJS + Git
- [ ] 模块结构 + Swagger + API Prefix
- [ ] PostgreSQL + Prisma + Migration
- [ ] Docker Compose + Redis
- [ ] 前后端联调（GET /api/v1/tools）

### Sprint 2（用户 + 第一个工具）

- [ ] 注册 / 登录 / JWT / 获取用户
- [ ] 工具列表 + JSON Formatter（格式化 / 错误提示 / 复制）
- [ ] User / Tool / ToolHistory 三表
- [ ] 路由守卫

### Sprint 3（AI 能力）

- [ ] Provider 模式（DeepSeek / GLM / Gemini）
- [ ] Code Explain / Error Fix / Commit Generator
- [ ] Token 统计 + 额度限制
- [ ] 安全（Key 不外泄 / 输入限制 / Prompt 防注入）

### Sprint 4（上线）

- [ ] 生产 Dockerfile
- [ ] Nginx + Cloudflare + 域名 + HTTPS
- [ ] Jenkins CI/CD
- [ ] SEO + Google Analytics
- [ ] 正式发布

---

## 六、开发原则与 Trae 使用要点

1. **产品驱动学习**：不孤立学技术，通过做功能自然掌握（如做 JSON Formatter 顺带学表单/API/Controller/校验/部署）。
2. **不要让 AI 一次生成整个项目**：按模块拆分 Prompt，先输出方案再生成代码。
3. **Prompt 三类用法**：
   - 架构任务：先分析设计，再改代码
   - 功能任务：明确技术栈 + 要求 + 组件行数限制
   - Review 任务：重点查架构 / 安全 / 性能 / 可维护性
4. **Git 规范**：Conventional Commit（feat / fix / refactor / chore），分支 main / develop / feature-xxx。
5. **时间建议**：工作日每晚 20:30-22:30（2h），周末 4-6h。

---

## 七、当前状态与下一步

- **已完成文档**：1~8 号文档完整，覆盖产品 → 技术 → 数据库 → 架构 → 30 天计划 → Sprint 1/2/3 实战。
- **待补充**：9 号文档（Sprint 4 生产部署）为空，建议优先编写 Dockerfile、Nginx 配置、Cloudflare 配置、Jenkins pipeline 实战步骤。
- **建议起点**：从 Sprint 1 的第一个 Commit 开始——`mkdir devpilot-toolkit && git init && pnpm init`，不要等。

---

## 八、项目蓝图

```
                DevPilot AI Toolkit
                         |
        --------------------------------
        |                              |
    免费开发工具                      AI 助手
   JSON / JWT / SQL                  Code Explain
   Regex / Base64                    Error Fix
   UUID / Timestamp                  Commit
   QR / Diff / Markdown              Test
                         |
                  用户体系（GitHub/Google/JWT）
                         |
                  Pro 会员（Stripe）
                         |
              API / Plugin / Enterprise
```
